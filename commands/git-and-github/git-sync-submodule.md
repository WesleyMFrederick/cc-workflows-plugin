---
description: "Sync .claude submodule to match the local hub"
---

# Execute Tasks
Sync .claude submodule to match the local hub using your `git-sync-submodule` skill

## Load Skill & Execute Plan
1. Create a sub-task using the `github-assistant` agent
2. Instruct agent to load `git-sync-submodule` skill
3. Instruct agent to execute the 8-step sync process
